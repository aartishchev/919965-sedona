Критерий принимаю. Но чуть корректнее было сделать так: во-первых добавить в поля input аттрибут required чтобы запретить отправку формы с пустыми полями. Во-вторых когда вы находите datetocome.value datetoleave.value - они у вас по умолчанию прописаны в разметке и не нулевые.

Ваш код работал бы правильнее в таком случае 

form.addEventListener ("submit", function (evt) {
evt.preventDefault();
if (!datetocome.value || !datetoleave.value || !adults.value) {
form.classList.add ("error-modalwindow");
return;
} else {
form.classList.remove ("error-modalwindow");
form.submit();
}
});
Понимаете? отменяем отправку формы по умолчанию в любом случае, и отправляем ее только если поля не пусты. А в идеале было бы еще добавить слушатель на изменение значения поля и сверять введенные данные с неким правильным шаблоном. Но это уже более продвинутый уровень.